# invoke with: ansible-playbook rpi64box.yml
# on macOS with ansible 2.3.1 and later, you have to install sshpass:
#   brew tap esolitos/ipa
#   brew install sshpass
---

- import_playbook: installsshkeys.yml
- import_playbook: buildrpi64box.yml
